<div class="product-missing">
  <div>
    <p class="eyebrow">404</p>
    <h1 class="section-title" style="margin-bottom:16px">{{ 'general.404.title' | t }}</h1>
    <p style="color:var(--ivory-dim);margin-bottom:28px">{{ 'general.404.subtext' | t }}</p>
    <a class="btn btn-primary" href="{{ routes.root_url }}">{{ 'general.404.link' | t }}</a>
  </div>
</div>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
